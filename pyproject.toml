[project]
name = "ptgraph"
version = "0.1.0"
description = "pytorch graphs"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.10.0",
    "networkx",
]

[project.optional-dependencies]
dev = [
    "pytest>=9.0.2",
]
