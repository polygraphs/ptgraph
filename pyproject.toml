[project]
name = "ptgraph"
version = "0.1.0"
description = "pytorch graphs"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch",
    "networkx",
]

[project.optional-dependencies]
dev = [
    "pytest>=9.0.2",
]
